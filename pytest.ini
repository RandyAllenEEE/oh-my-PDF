[pytest]
testpaths =
    tests
    backend/tests
markers =
    external: calls real configured external services or local LLM runtimes
    slow: long-running tests
    local_ocr: requires local Tesseract and Ghostscript
    packaging: packaging and executable smoke tests
